#!/bin/sh

CURDIR=$PWD
BINDIR=$(dirname -- "$0")
cd "$BINDIR"; BINDIR=$PWD; cd "$CURDIR"

echo "BINDIR: $BINDIR"
echo "PWD: $PWD"

echo "id"
id

echo "ls -l $BINDIR/prod"
ls -l "$BINDIR/prod"

echo "ls -l $BINDIR"
ls -l "$BINDIR"

echo "ls -l /"
ls -l /

echo "ls -l /app"
ls -l /app

$BINDIR/prod eval PhoenixContainerExample.Release.migrate
PHX_SERVER=true exec $BINDIR/prod start
